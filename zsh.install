#!/usr/bin/env sh

post_install() {
  grep -qe '^/usr/bin/zsh$' etc/shells || echo '/usr/bin/zsh' >> etc/shells
}

post_upgrade() {
  post_install
}

pre_remove() {
  sed -ir '/^\/usr\/bin\/zsh$/d' etc/shells
}
