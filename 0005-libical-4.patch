diff --git a/Makefile.obexd b/Makefile.obexd
index 7ad74e1..b05cc68 100644
--- a/Makefile.obexd
+++ b/Makefile.obexd
@@ -93,7 +93,7 @@ obexd_src_obexd_SOURCES = $(btio_sources) $(gobex_sources) \
 obexd_src_obexd_LDADD = lib/libbluetooth-internal.la \
 			gdbus/libgdbus-internal.la \
 			src/libshared-glib.la \
-			$(ICAL_LIBS) $(DBUS_LIBS) $(LIBEBOOK_LIBS) \
+			$(ICAL_LIBS) $(ICALVCAL_LIBS) $(DBUS_LIBS) $(LIBEBOOK_LIBS) \
 			$(LIBEDATASERVER_LIBS) $(GLIB_LIBS) -ldl
 
 if EXTERNAL_PLUGINS
diff --git a/configure.ac b/configure.ac
index 52de7d6..2548273 100644
--- a/configure.ac
+++ b/configure.ac
@@ -307,6 +307,7 @@ AC_ARG_ENABLE(obex, AS_HELP_STRING([--disable-obex],
 		[disable OBEX profile support]), [enable_obex=${enableval}])
 if (test "${enable_obex}" != "no"); then
 	PKG_CHECK_MODULES(ICAL, libical)
+	PKG_CHECK_MODULES(ICALVCAL, libicalvcal)
 fi
 AM_CONDITIONAL(OBEX, test "${enable_obex}" != "no")
 
